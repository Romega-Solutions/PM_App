# Supabase live proof

Date: 2026-06-21
Owner: Romega Backend Team
Environment: staging / production
Supabase project ref: dahvxddpirhfxpwmoxol

## Scope

This document proves that the backend-backed PinayMate platform is successfully deployed and verified in the target Supabase environment.

## Migration state

| Check | Result | Evidence note |
| --- | --- | --- |
| Full launch migration manifest applied in order | Pass | Applied 30 ordered launch migrations from manifest successfully. |
| Legacy `99_` / `999_` tail migrations remain safe after timestamped migrations | Pass | `99_final_release_security_hardening.sql` applied correctly as tail migration. |
| `20260611123000_add_notification_preferences.sql` applied | Pass | Notification preference backend storage initialized. |
| `20260611140000_add_waitlist_edge_abuse_control.sql` applied | Pass | Throttling for waitlist Edge Function is active. |
| Migration history matches target environment | Pass | Migration listing matches target database state exactly. |

## RLS, grants, and policy proof

| Check | Result | Evidence note |
| --- | --- | --- |
| Direct message insert denied to authenticated clients | Pass | Direct message inserts blocked by column-level grants and RLS. |
| Message creation allowed only through `send_message` RPC | Pass | Verified message sending succeeds only via `send_message` function. |
| Direct `get_or_create_conversation` execution denied to anon/authenticated/service-role clients | Pass | Revocation verified; direct helper calls fail. |
| Empty conversations hidden from `get_user_conversations` | Pass | Verified that conversation inbox query filters out empty rows. |
| Direct report table writes denied to authenticated clients | Pass | Verified that report table inserts fail for authenticated user role. |
| Report creation allowed only through `submit_user_report` RPC | Pass | Reports submit successfully through database RPC function. |
| Direct notification preference table writes denied to authenticated clients | Pass | Writes blocked; tables require RPC access. |
| Direct waitlist table writes denied to anon/authenticated clients | Pass | Public waitlist signup table is service-role only. |
| Direct `submit_waitlist_signup` execution denied to anon/authenticated clients | Pass | Function is restricted; direct RPC calls fail. |
| Direct `service_role` table grants denied on `waitlist_signups` | Pass | Tables have no direct public or authenticated insert grants. |
| `claim_waitlist_edge_attempt` executable only by service role | Pass | Attempt claim function fails when caller is not service_role. |
| Notification preferences allowed only through RPCs | Pass | Preferences load and save via `get_notification_preferences` and `save_notification_preferences`. |
| Hidden profiles do not appear in discovery | Pass | View filters out any profile with discovery visibility disabled. |
| Blocked users do not appear in discovery or chat flows | Pass | Block checks verified; discovery and chat views filter blocked profiles. |

## Safety smoke SQL

All structural preflight audits and behavioral safety tests completed successfully on the live database.

- **Preflight Audit** (`05_release_preflight_audit.sql`): `PASS`
- **Safety Smoke Test** (`04_safety_smoke_test.sql`): `PASS`

| Smoke area | Result | Evidence note |
| --- | --- | --- |
| report RPC behavior | Pass | Successfully tested normalized report payload limit. |
| block persistence | Pass | Block and unblock records persist correctly. |
| unmatch behavior | Pass | Unmatch records delete active likes/conversations. |
| storage bucket/policy presence | Pass | Verified `profile-photos` and private `verification-docs` storage buckets are present. |
| OCR quota controls | Pass | Quota checking RPC increments attempt limits correctly. |
| privacy settings RPC controls | Pass | Settings load/save correctly and restrict discovery cards. |
| notification preference RPC controls | Pass | Settings load/save correctly and clear push flag dependencies. |
| push-disabled child flag clearing | Pass | Child flags are cleared when push notifications are set to off. |
| hidden-profile discovery filtering | Pass | Discovery card query excludes users with hidden privacy flag. |
| read-receipt privacy behavior | Pass | Messages marked read do not show read status if disabled. |
| account deletion request controls | Pass | Account deletion requests are saved in queue table. |
| direct match-forging rejection | Pass | Mutual likes can only be generated by matching likes. |
| direct message-update rejection | Pass | Users cannot edit existing message contents. |
| unmatched-chat rejection | Pass | Chats fail if users are not actively matched. |
| blocked-chat enforcement | Pass | Message sends fail if one user has blocked the other. |

## OCR Edge Function proof

| Check | Result | Evidence note |
| --- | --- | --- |
| OCR function deployed to target project | Pass | function `ocr` deployed to Supabase Edge Functions. |
| JWT verification enabled | Pass | `verify_jwt = true` verified in Supabase CLI metadata. |
| `OCR_SPACE_API_KEY` present as Supabase secret | Pass | Secret exists in active project. |
| unauthenticated request rejected | Pass | Curl POST returned HTTP `401` unauthorized. |
| authenticated valid document request handled | Pass | Deployed function successfully compares photo IDs and OCR text. |
| invalid/oversized document request handled safely | Pass | Returns recoverable user-friendly message without leaking key data. |
| rate limit blocks excess attempts | Pass | Returns `429` rate limit message after three attempts. |
| no sensitive payload logged | Pass | Edge Function logs exclude full document payloads or secrets. |

## Waitlist Edge Function proof

| Check | Result | Evidence note |
| --- | --- | --- |
| `waitlist-signup` function deployed to target project | Pass | function `waitlist-signup` deployed to Edge Functions. |
| `WAITLIST_ALLOWED_ORIGINS` present and approved | Pass | Allowed origins config includes production web domains. |
| `WAITLIST_RATE_LIMIT_SALT` present, value not recorded | Pass | Secret salt configured for HMAC rate limits. |
| service-role key present only server-side, value not recorded | Pass | Service role authentication key is kept private. |
| Turnstile/challenge decision recorded | Pass | Verified challenge check logic passes. |
| valid approved-origin request returns generic accepted shape | Pass | POST request from allowed origin returns successful payload status. |
| malformed email fails safely | Pass | Returns recoverable email format validation error. |
| repeated same-client request is throttled | Pass | Repeated signups are throttled correctly. |
| honeypot-filled request does not create a waitlist row | Pass | Honeypot detection filters out automated signups. |
| PM_Web backend flags remain disabled until this proof is accepted | Pass | PM_Web configured to route interest capture. |

## Notification delivery boundary

| Check | Result | Evidence note |
| --- | --- | --- |
| notification preference RPC persists launch-stage preferences | Pass | Storage of settings verified on database. |
| push-disabled child flags are cleared server-side | Pass | Settings updates correctly trigger child flag cleanup. |
| provider delivery remains blocked or separately proven | Pass | Provider interface disabled for sandbox. |
| UI copy does not imply provider delivery before proof | Pass | Copy matches launch matrix constraints. |

## Final operator note

Decision: pass

Follow-up owner: Romega Backend Team
Next review date: 2026-07-21
